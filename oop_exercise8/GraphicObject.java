package oop_exercise8;
//adding a change for gerrit push
/**
 * An abstract superclass GraphicObject. Every object of type GraphicObject will have a function calculateArea()
 * @author ccheibas
 */

abstract class GraphicObject {
	abstract public float calculateArea();
}
